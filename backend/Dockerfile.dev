# 开发环境专用Dockerfile
FROM node:22-alpine

# 设置工作目录
WORKDIR /app

# 安装开发工具
RUN npm install -g nodemon ts-node

# 复制package文件
COPY package*.json ./

# 安装所有依赖（包括开发依赖）
RUN npm install

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 开发模式启动（支持热重载）
CMD ["npx", "nodemon", "--exec", "npx ts-node --transpile-only src/app.ts"]